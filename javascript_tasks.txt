css
add breakpoints, media queries for a iPhone size document

javascript
drag image functionality
table headers - set pre defined ones, optional ones and saved layouts under dropdown.
once slected, table headers will display accordingly.
any personalised layout created must be given a name, then 'confirm' button to save
under dropdown buttons to 'create' new layout and 'modify' existing layouts
table header selections for baptisms, marriages and burials / checkboxes then display the layout accordingly with selected table headers
functionality so each user can save the table header selection for next time
activate saved layout types from the user (under the event types) and apply to table headers - user can create  / personalise any number of layouts as he likes)
if no layouts have been created, default table headers used
